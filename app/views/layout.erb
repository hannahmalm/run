<!DOCTYPE html>
<html>
<head>
    <title>Running Log</title>
    <style>
      body{
        background-color: #FAEBD7;
        text-align: center;
        font-family: arial, sans-serif;
        font-weight: normal;
    }

    </style>
</head>
    <body>
        <% if logged_in? %>
            <a href="/logout">Logout</a>
         <% end %>
        <%= yield %>
    </body>
</html>